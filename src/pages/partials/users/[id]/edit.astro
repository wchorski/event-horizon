---
// partials/users/:id/edit
import { tableConfigs } from "@lib/tableConfigs";
import { BadRequestError, errorHandlingOnSubmit } from "@lib/errors";
import { crud, type CrudRegistryType } from "@lib/crudRegistry";
import { parseOutUndefinedFormData } from "@lib/sanatizers";
import { TEST_ADMIN_SESSION } from "@lib/auth/session";
import type { UserSelect } from "@ty/Schema";
import RowEdit from "@components/tables/RowEdit.astro";

let user: UserSelect | null | undefined = null;
let error = null;

const { id } = Astro.params;
if (!id) throw new BadRequestError("user ID missing in request");
const config = tableConfigs.users.required;
const ctx = {
  crud: "users" as CrudRegistryType,
  config,
  endpoint: "/users",
  headers: Object.keys(config),
};

if (Astro.request.method === "GET") {
  try {
    // const session = await requirePermission(Astro.request, "users", "create");
    // TODO

    const session = TEST_ADMIN_SESSION;

    user = await crud.users.read(id, session);
  } catch (e) {
    const { err, status } = errorHandlingOnSubmit(e);
    Astro.response.status = status;
    error = err;
    console.log(JSON.stringify(err, null, 2));
  }
} else {
  return new Response("Method Not Allowed", { status: 405 });
}

enum PartialType {
  TableRow = "table-row",
  SinglePage = "single-page",
}

const partialType =
  (Astro.request.headers.get("X-Partial-Type") as PartialType) ||
  PartialType.TableRow;
---

{
  error ? (
    <div id={`top-level-error-${ctx.crud}-${id}`}>
      {typeof error === "string" ? error : JSON.stringify(error)}
    </div>
  ) : user && partialType === PartialType.TableRow ? (
    <RowEdit {...ctx} row={user} error={error} />
  ) : user && partialType === PartialType.SinglePage ? (
    <button type="button" id="submit-button" disabled>
      ✓ saved
    </button>
  ) : (
    <p>unknown error occured</p>
  )
}